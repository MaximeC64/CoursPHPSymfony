{% extends 'base.html.twig' %}

{% block title %}New Categorie{% endblock %}

{% block body %}
<div class="offset-3 col-sm-6 align-content-center">
    <div class="card">
        <div class="card-header">
            <h1>Create new Categorie</h1>
        </div>
        <div class="card-body">
            {{ include('categorie/_form.html.twig') }}

            <a class="btn btn-dark" href="{{ path('categorie_index') }}">Retour</a>
        </div>
    </div>
</div>
{% endblock %}
